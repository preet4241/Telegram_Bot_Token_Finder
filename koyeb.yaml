# Koyeb configuration
# For Go applications, Koyeb typically detects the build system automatically.
# This file provides guidance for manual setup if needed.

# Build Command: go build -o token-hunter .
# Run Command: ./token-hunter
# Port: 8080 or as per your environment
